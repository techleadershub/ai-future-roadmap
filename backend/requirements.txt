# ---------------------------------------------------------------------------
# PINNED 7 September 2026
#
# Every one of the six dependencies was completely unpinned (a bare package
# name), so each rebuild silently picked up whatever was newest on PyPI.
#
# These versions are a snapshot of the Python 3.13.11 environment that was running in
# production on 7 September 2026, captured with `pip freeze` inside the live container.
# They are what the service is known to work on -- not a fresh resolution.
#
# A `>=` bound is not a pin. It re-resolves on every rebuild, and migrating a
# service *is* a rebuild.
# ---------------------------------------------------------------------------

annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
certifi==2026.1.4
charset-normalizer==3.4.4
click==8.3.1
distro==1.9.0
fastapi==0.128.0
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.11
jiter==0.12.0
jsonpatch==1.33
jsonpointer==3.0.0
langchain-core==1.2.7
langchain-openai==1.1.7
langchain==1.2.6
langgraph-checkpoint==4.0.0
langgraph-prebuilt==1.0.6
langgraph-sdk==0.3.3
langgraph==1.0.6
langsmith==0.6.4
openai==2.15.0
orjson==3.11.5
ormsgpack==1.12.1
packaging==25.0
pydantic==2.12.5
pydantic_core==2.41.5
python-dotenv==1.2.1
PyYAML==6.0.3
regex==2026.1.15
requests-toolbelt==1.0.0
requests==2.32.5
sniffio==1.3.1
starlette==0.50.0
tenacity==9.1.2
tiktoken==0.12.0
tqdm==4.67.1
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.3
uuid_utils==0.13.0
uvicorn==0.40.0
xxhash==3.6.0
zstandard==0.25.0
